const evens = (array) => {
  let r = [];
  for (let i = 0; i < array.length; i++) {
    if (array[i] % 2 === 0) {
      r.push(array[i]);
    }
  }
  return r;
};

const shouty = (array) => {
  let r = [];
  for (let i = 0; i < array.length; i++) {
    r.push[i].toUpperCase;
  }
  return r;
};

const join = (array) => {
  return
}

const allSiblings = (array) => {
  let r = []
  for (let i = 0; i < array.length; i++) {
    if 
}
}

const strange = (array) => {
  return strange.map
}